using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// A container for control vlaues so they don't get serialized only in components
public class ControlValueGO : MonoBehaviour
{
    public ControlValue _CV;
}
